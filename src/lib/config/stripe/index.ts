// import Stripe from 'stripe';
// import { STRIPE_SECRET } from '../env';

// const options: Stripe.StripeConfig = {
//   apiVersion: '2023-10-16',
//   appInfo: {
//     name: 'Selleryo',
//     version: '0.1.0',
//   },
// };

// export const stripe = new Stripe(STRIPE_SECRET, options);
